version: 2.1
orbs:
  node: circleci/node@x.y
jobs:
  build:
    docker:
      - image: 'cimg/base:stable'
    steps:
      - checkout
      - node/install:
          install-yarn: true
          node-version: latest
      - run: node --version
workflows:
  build:
    jobs:
      - build